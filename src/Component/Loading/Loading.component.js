// @flow

import React from 'react';

import {
  StyledContentContainer,
  LoadingText
} from './Loading.styles';

const Loading = () => (
  <StyledContentContainer>
    <LoadingText>Loading...</LoadingText>
  </StyledContentContainer>
);

export default Loading;
