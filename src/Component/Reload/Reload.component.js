// @flow

import React from 'react';

import {
  StyledContentContainer,
  ReloadText
} from './Reload.styles';

const Reload = () => (
  <StyledContentContainer>
    <ReloadText>Click reset to refetch or display home page</ReloadText>
  </StyledContentContainer>
);

export default Reload;
